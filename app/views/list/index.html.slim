- @title ||= ti(:title, models: models_label)

- content_for(:search, render('search')) if search_support?
- content_for(:filter, render('filter')) if filter_support?
- content_for(:actions, render('actions_index'))

= render 'list_with_header'
